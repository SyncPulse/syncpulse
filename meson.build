project('syncpulse', 'c',
  version : '0.1',
  default_options : ['warning_level=3'])

# build syncpulse main
syncpulse_option = get_option('syncpulse')
if syncpulse_option.enabled()
  subdir('syncpulse')
endif
